apply plugin: 'groovy'
apply plugin: 'maven'

group = "bravo"
version = "0.1"

dependencies {
    compile gradleApi()
}

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: mavenLocal().url)
        }
    }
}






